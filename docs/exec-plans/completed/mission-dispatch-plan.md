# Mission dispatch plan

Add a read-only dispatch contract for runnable DAG nodes. Each contract records dependencies, risk-derived roles, isolated-worktree requirement, and verification breadth. No runtime is launched.

Verification: mission work-graph tests, strict control-plane validation, and full KEEL verification.
