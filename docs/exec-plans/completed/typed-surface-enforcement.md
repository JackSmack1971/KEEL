# Typed implementation-surface enforcement

Connect optional implementation-surface metadata to material changed paths during evidence evaluation. Keep metadata optional for legacy contracts and fail closed only when a surface is explicitly declared but unmatched.

Verification: evidence-graph tests, strict control-plane validation, and full KEEL verification.
