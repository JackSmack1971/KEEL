# Lifecycle telemetry

Add a read-only projection over KEEL verification results. Report measured check counts, pass/fail counts, wall time, and command count; represent tokens, cost, human intervention, retries, and merge conflicts as unavailable unless a runtime supplies them.

Verification: focused telemetry tests, strict control-plane validation, and full KEEL verification.
