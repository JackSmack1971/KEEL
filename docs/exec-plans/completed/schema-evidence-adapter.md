# Schema evidence adapter

Implement a narrow native adapter for repository-contained JSON schema facts: parseability, expected `schema_version`, and declared required keys. Keep external/runtime schema providers command-backed.

Verification: provider contract tests, strict control-plane validation, and full KEEL verification.
