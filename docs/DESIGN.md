# Design

Design decisions must be discoverable, versioned, and distinguish current facts from proposals. Use `docs/design-docs/` for decisions that materially affect architecture, interfaces, data, operations, security, or user-visible behavior.

Prefer constraints that protect outcomes over instructions that dictate style. If a repeated failure survives prose guidance, consider a lint, architecture test, schema, generator, or other deterministic safeguard.
