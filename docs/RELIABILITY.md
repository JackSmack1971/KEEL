# Reliability

Status: `CONDITIONAL — activate when a runtime/service/device/system has measurable availability or correctness obligations.`

When activated, define user-visible failure modes, SLOs/SLIs or equivalent acceptance budgets, graceful degradation/fail-safe behavior, retry/idempotency policy, dependency failure behavior, observability evidence, and recovery/rollback tests.
