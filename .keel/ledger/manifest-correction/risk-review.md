# Risk review

Control-plane provenance and verification configuration are changed. The scope is limited to the manifest hash and the local KEEL doctor command; no external effects, secrets, production systems, or irreversible operations are involved. The change is reversible by restoring the prior tracked values.
