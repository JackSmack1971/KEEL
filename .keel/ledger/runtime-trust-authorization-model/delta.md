## ADDED
- First-class conservative runtime observations and runtime-profile collection.
- Intent-bound capability grants, independent effect requests, effect adapter/receipt contracts, and deterministic trust-policy decisions.
- Hostile fixtures for absent/stale/conflicting runtime evidence, incomplete enforcement, authorization mismatches, expiry/use exhaustion, and legacy compatibility.

## MODIFIED
- Canonical primitive contracts gain the fields needed to bind effects and grants without boolean authorization semantics.
- Existing effects/authorization inputs are read through conservative compatibility adapters that never invent grants.
- Durable architecture, decision, workflow, and command documentation describes the implemented M4 boundary and its limitations.

## REMOVED
- Boolean authorization as permission authority; legacy boolean records remain readable only as non-grant compatibility evidence.
