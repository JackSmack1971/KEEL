## ADDED
- Add a dependency-free canonical semantic model for the eleven primitive kinds, orthogonal state dimensions, identities, provenance, resources, schemas, deterministic serialization/digests, and collection validation.
- Add hostile independent tests for strict decoding, portability, malformed records, duplicate identities, determinism, and provenance integrity.

## MODIFIED
- Register the semantic-kernel test as a required canonical verification command and regenerate its declared bootstrap manifest through the producer.
- Record a durable M1 implementation plan and current architecture fact.

## REMOVED
- Preserve P0 portability behavior on POSIX by narrowly recognizing Windows absolute creator-machine command paths.
