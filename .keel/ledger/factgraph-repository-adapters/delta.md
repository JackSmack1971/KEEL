## ADDED

- A canonical deterministic FactGraph and generic read-only RepositoryAdapter SPI.
- Foundational filesystem/Git plus bounded ecosystem adapters, cross-stack fixtures,
  source-digest freshness, conflict visibility, and graph impact queries.

## MODIFIED

- Repository intelligence becomes a compatibility projection of canonical graph facts.
- Repository map, capability resolution, and context inputs consume FactGraph-derived
  compatibility views instead of maintaining overlapping repository authority.
- Architecture/capability documentation records the canonical boundary and adapter rules.

## REMOVED

- Duplicate repository/source/capability detection authority in legacy projection modules.
