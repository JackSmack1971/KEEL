# KEEL ledger

One directory per change-id. Ledger files are normal Git-tracked evidence and should land with the change they govern. Runtime-local `active-change` and audit dedupe state are ignored by `.keel/.gitignore`.
