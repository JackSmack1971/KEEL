# Risk review

This is a documentation/control-plane-only change. It does not execute commands beyond repository verification, alter runtime policy, contact external systems, or authorize deferred integrations. The acceptance matrix requires coverage of every unresolved audit row and explicitly records blockers rather than converting them into unsupported claims.

Not required for ordinary standard/trivial changes. Required before plan gate for high/control-plane/security/privacy/migration/release/high-blast-radius changes.
