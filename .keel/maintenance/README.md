# Optional maintenance tools

This directory contains read-only repository gardening and feedback utilities. It is
not part of the KEEL kernel, lifecycle authority, normal CLI, or runtime adapter path.
Maintenance output is advisory input to a separately governed change.

Run `python3 .keel/maintenance/keel_maintenance.py --help` to inspect the optional
commands.
